# Elementary HPO

A lightweight hyperparameter optimization tool using Quasi-Monte Carlo (Sobol) sequences. This package allows you to optimize `Scikit-Learn Models` hyperparameters efficiently by covering the search space more evenly than random search.

## Installation

```bash
pip install elementary_search